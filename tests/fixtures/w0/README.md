# W0 golden fixtures

These fixtures freeze representative output contracts for the Community 2.0
refactor. They are intentionally small and deterministic; volatile OpenCTI
timestamps, generated IDs and local paths are excluded. The existing unit
tests remain the primary behavioral suite.
